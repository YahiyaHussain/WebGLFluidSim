export enum OperationType {
  Add,
  Subtract,
  Multiply,
}

export function getDataOperationFragSrc(operation: OperationType) {
  switch (operation) {
    case OperationType.Add:
      return;
  }
}
